version: '3.9'
services:
  mongodb:
    image: mongo:5.0
    ports:
      - 27018:27017
    volumes:
      - ~/apps/mongo:/data/mongodb
    environment:
      - MONGO_INITDB_ROOT_USERNAME=nigelcuschieri
      - MONGO_INITDB_ROOT_PASSWORD=examplePassword123!